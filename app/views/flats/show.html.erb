<h1>this is show</h1>
<%= @flat.name %>

<p><%= @flat.address %></p>
<p><%= @flat.description %></p>
<p><%= @flat.price_per_night %></p>
<p><%= @flat.number_of_guests %></p>

<%= link_to "Back", flats_path(@flat)%>
<%= link_to "Edit", edit_flat_path(@flat)%>
<%= link_to "Remove", flats_path(@flat)%>

